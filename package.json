{
  "name": "graphology.do.cf",
  "module": "worker.js",
  "version": "1.0.0",
  "description": "A Graphology implementation using Cloudflare Worker Durable Objects.",
  "repository": "git@github.com:drivly/graphology.do.cf.git",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev worker.js",
    "deploy": "wrangler publish worker.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "dependencies": {
    "graphology": "^0.24.1"
  },
  "devDependencies": {
    "jest": "^28.1.3",
    "prettier": "^1.17.0",
    "wrangler": "^2.0.27"
  }
}
